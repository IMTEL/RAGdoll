from fastapi import APIRouter


@router.get("/api/progress/initializeTasks")
def getLLMs():
    """
    Returns the available LLMs which can be used by the
    """
    return {
        
    }
        